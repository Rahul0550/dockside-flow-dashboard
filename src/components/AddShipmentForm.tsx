
// Re-export the AddShipmentButton for backward compatibility
import { AddShipmentButton } from "./shipment/AddShipmentButton";

export function AddShipmentForm() {
  return <AddShipmentButton />;
}
